# Only track .py and .ipynb files
# Ignore everything by default
*

# But allow .py and .ipynb files
!*.py
!*.ipynb

# Allow directories (needed for git to traverse them)
!*/

# Allow this .gitignore file itself
!.gitignore

# Allow README files (optional - you can remove these lines if you don't want them)
!README.md
!readme.md
!README.txt
!readme.txt

# Still ignore these even if they match the above patterns
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints/
credit-rv/
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
*.so
*.dylib
*.dll
